# 船体与分区

设施是一艘船。地形本身是可读的，`python3 tools/show_terrain.py` 会把平面图、放置物件和所有区域一起打出来，并报出每个区域占甲板的百分比——**"这些区是不是我想要的大小"是画分区图唯一要回答的问题**，所以那一栏就在图例里。

| | |
|---|---|
| 地图 | 192×192 格 |
| 可玩区（playable area） | x 10..182、y 8..172 |
| **镜头范围（camera bounds）** | **x 17..175、y 12..168** |
| 可走甲板（镜头内） | 17,176 格 |
| 船长 | 约 156 格 → 员工（速度 2.25）**从头走到尾 69 秒** |
| 地形集 | `KorhalPlatformEx2` |

> **可玩区不是镜头范围，差一圈。**可玩区四边各比镜头多出 7 格（横）/ 4 格（纵），所以按可玩区画出来的平面图带着一圈**镜头永远够不到的边**，船看起来比实际能玩的更长。
>
> **`MapInfo` 里只有可玩区那一个矩形**（偏移 64 起，四个 8 位定点数）。镜头范围在编辑器里单独设，而且它的数值**在地图的任何文件里都搜不到**——按整数、按定点、按浮点都搜过。所以 `show_terrain.py` 默认画可玩区并且如实标出来，要看真的就 `--bounds 17 12 175 168`。

## 六个区

**分区和部门解耦。**任何部门都可以坐在任何一个区里。唯一的倾向是**中央本部偏好 `zone_amidships`**——它的技能是全船性的，坐在中间最好使——而且这只是倾向，不是约束。

所以区的名字用航海方位，不用部门名。

按镜头范围量（`--bounds 17 12 175 168`）：

| 区域 | 甲板占比 | 意图 |
|---|---|---|
| `zone_amidships` | **20.4%** | 稍大 |
| `zone_aft_port` | 17.9% | 正常 |
| `zone_aft_starboard` | 17.9% | 正常 |
| `zone_bow` | 16.9% | 正常 |
| `zone_engine_port` | **13.5%** | 偏小 |
| `zone_engine_starboard` | **13.5%** | 偏小 |

六块铺满，左右完全镜像。最大/最小 1.51×，而大的小的正是想让它大和小的那两个。**镜头范围只切掉全船 2% 的甲板，六个比例挪动都不到半个百分点**，所以这套分区对"按可玩区量还是按镜头量"不敏感。

**它们是矩形，而且故意是矩形。**边界最终要由舱壁来落实；这六个矩形是**给舱壁用的标线**，在编辑器里拖一下就能改，改完 `show_terrain.py` 立刻重新报数。

> **矩形的外缘故意画到了可玩区边上（10 / 8 / 182 / 172），比镜头范围还宽。**外溢是无害的——只有落在甲板上的格子会被算进去——而且**船体纵向一加高，这几个区不用改就还能罩住新甲板**。真正要紧的只有内部那几条分界：x=96、x=65|66、x=125|126、y=73|74、y=132|133。

> **这六个区不是防御地点。**`Fort_BuildRegistry` 只收名字以 `room_` 或 `corridor_` 开头的区域，所以 `zone_` 前缀天然不会污染 `gvg_loc*` 那张表。同理 `resthall1` 也不是——它是休息室，不是可升级的房间。

## 船体自己的形状

**左右镜像，前后不对称。**六个区在这种形状上只有一种自然分法：**两个骑在中轴上的区，加两对镜像**。上表正是——`bow` 和 `amidships` 在中轴，两个 `engine_*` 一对，两个 `aft_*` 一对。这不是凑出来的，是船形逼出来的。

一个对中央本部有用的副产品：**`amidships` 同时和其余五个区都接壤**（前接 `bow`、左右接两个 `engine_*`、后接两个 `aft_*`）。支援型部门坐这儿，去任何地方都不用穿过第三个区。

## 唯一一条硬性地形要求：把中后舱壁往前推

**船体自己的腰在 y≈56，而尺寸需要的分界线在 y=74。**差 18 格，而这 18 格把结果整个翻过来：

| 中后分界线 | bow | amidships | engine ×2 | aft ×2 |
|---|---|---|---|---|
| **y=74**（现在这套） | 16.9% | 20.6% | **13.5 / 13.2** | **17.9 / 17.9** |
| y=66 | 16.9% | 23.5% | **15.1 / 14.8** | **14.9 / 14.9** |
| **y=56**（船体的腰） | 16.9% | 27.0% | **16.7 / 16.3** | **11.5 / 11.5** |

**顺着现有的腰切，引擎侧会变成除中央外最大的，后两区会变成全船最小的——正好和意图相反**，中央也会胀到 27.0%，那已经不叫"稍大"。

所以中后舱壁要落在 **y≈74**，而不是船体现在收腰的地方；或者让后两区沿侧舷越过那道腰往前伸。

## 五条边界，已经画好的只有一条

| 边界 | 状态 |
|---|---|
| `bow` / 中段：y=133 | ✅ **船体在这里从 144 宽收到 120**，`##########` 肩部已经在了。全船最干净的一条，白送 |
| `amidships` / `engine_port`：x=66，y 74..132 | 开阔地 |
| `amidships` / `engine_starboard`：x=126，y 74..132 | 开阔地 |
| 中段 / 后段：y=74，通宽 | 开阔地，而且和船体的腰打架（见上） |
| `aft_port` / `aft_starboard`：x=96，y 8..73 | 开阔地——不过**中轴纵舱壁在船上非常自然**，这条最该画 |

## 先给中央一个轮廓

整个中段 57% 的面积里，**唯一的内部结构就是那两个引擎盒**（x 55-66 和 x 125-136，y 96-110，各带一个 6×6 的空腔）。

它们位置极好：正压在 `amidships` 和两个 `engine_*` 的分界外侧，**引擎侧那个区天生就有锚点**，所以"引擎侧偏小"这件事地形已经在帮忙了。

而 **`amidships`——最大、也最需要一眼认得出来的那个区——一格特征都没有。**中央本部要坐在这儿，五个方向都通向它，它却是全船最没有形状的地方。

**只改一处的话，先给中央一个轮廓。**它一成形，`amidships/engine_*` 那两条边界就同时有了，五条里去掉两条。

## 一个必须记住的约束：`Sector_OwnerNear` 穿墙

**归属是"最近的核心"，纯欧氏距离，不认墙**（`09_sector.galaxy`）。`Fort_OwnerOf`、`Cont_OwnerOf` 的兜底、`50_waves` 的哨位归属都走它。

所以砌完舱壁之后，**核心的位置必须让 Voronoi 的分界线落在舱壁上**。落不上，就会出现一间物理上在 A 区、却答应 B 部门的房间——而且**全场没有任何提示**：那个部门升级不了自己的房间，考验刷在别人家，防御任务算错数。

还有一条更省事的路：**让地点的归属跟着区域走，而不是跟着距离走**——区域名本来就是一条通道（`Fort_LocByName` 已经在用），让 `room_*` / `corridor_*` 落在哪个 `zone_*` 里就归谁。这不是新开通道。等舱壁定型再决定。
